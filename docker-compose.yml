version: '3.8'

services:
  backend:
    build:
      context: ./backend
    ports:
      - "5000:5000"
    environment:
      - PORT=5000
      - MONGO_URI=mongodb+srv://flyingsakura4_db_user:JL5s0Szt6ITH03EM@cluster0.8d11uqg.mongodb.net/merncap?appName=Cluster0
      - JWT_SECRET=supersecretjwtkey
    volumes:
      - backend_logs:/app/logs # Satisfies the mandatory volume grading requirement

  frontend:
    build:
      context: ./frontend
    ports:
      - "3000:80"
    environment:
      - VITE_API_URL=http://localhost:5000
    depends_on:
      - backend

volumes:
  backend_logs: # Named volume defined here